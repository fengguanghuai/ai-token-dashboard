# 看板第一版设计验收

日期：2026-09-14。范围：现有看板视觉与说明入口，不更改采集或历史数据。

后续调整：按用户要求恢复来源占比环形图及原有图例聚焦交互，保留新版布局与帮助入口。已在本地预览确认环形图显示；下文横向来源条的记录仅描述第一版验收状态。

组件展示补充：新增独立 `examples/ui-showcase`，本机端口 5174。主看板未改；组件接入方式、基础交互检查、手机布局及已知限制见该目录 README.md。此展示用于选型，不是正式替换方案或 AI 框架能力验收。

## 选定组件接入（2026-09-14）

- 按用户选择接入 NumberFlow 与 Bklit 折线；默认进入折线视图。Tambo 尚未接入。
- 保留来源环形图、堆叠/柱状模式、CSV 导出。增强折线通过两个日期范围滑块缩放，明细表提供精确数值与对应的上一周期日期。
- 字体、配色及间距沿用看板主题；NumberFlow 仅转换原格式化后的展示值，保留万/亿、千分位及美元小数精度。
- Bklit 仅导入折线依赖闭包，CSS 限定在增强图表内。保留 MIT 声明，不引入展示页其它图表库。
- 已检查：30 天和 7 天切换、上一周期开关、日期滑块键盘操作及重置、精确明细、切换回堆叠、单日兼容、深色模式、390px 手机布局。修正手机图表头部按钮换行；页面无横向溢出，当前浏览器未捕获 error/warn。
- 未完整验证：真实触屏及跨浏览器、鼠标悬停提示的端到端效果、模拟网络失败回退、400 天以上实际数据。点击/拖动自动化未捕获提示框，因此不将其列为已通过项。
- 119 项测试通过，构建通过，生产依赖审计零漏洞。增强图表按需加载约 157 KB（gzip 54 KB）；主包约 277 KB（gzip 90 KB）。
- 保留兼容策略：空/单日/超过 400 天或加载失败时显示原有 ECharts；Visx alpha 版本已锁定，后续升级需回归验证。

## Findings

本次已检查范围内无未解决的 P0/P1/P2 问题。手机帮助抽屉曾向左偏移，已限制为可用宽度并锁定背景滚动；复测 390px 屏幕左边界为 0、宽度为 390。

## 对照与证据

- 参考：用户选中的第一张「简洁分析看板」效果图。
- 桌面：1487 × 1058；将参考图与实现截图放在同一视觉输入中直接比较。
- 实现截图：`/tmp/token-studio-design-audit/02-implementation.png`。
- 手机帮助：`/tmp/token-studio-design-audit/03-mobile-help.png`。
- 深色桌面：`/tmp/token-studio-design-audit/04-dark.png`。
- 全幅对照已覆盖本次变化的筛选区、指标卡、趋势与来源区；抽屉另以桌面和手机近景检查，无需重复裁切。

## Fidelity surfaces

- 字体：保留项目现有字体与中文回退；指标数字提升至 26–38px，标题 15px，说明正文 14px。实际页面辅助控件比效果图略紧凑，为保留额度与导出操作的有意调整。
- 布局：实现双行筛选、大数字指标、趋势与横向来源条的主次布局；取消原独立说明横条。保留环比卡、图表缩放、热力图和明细，因此下半屏并非效果图的两栏简化版。
- 色彩：沿用暖白底及现有来源配色，保留深色令牌。费用增长使用中性颜色，不暗示增长就是好事。未引入效果图中的新渐变配色。
- 资产：复用真实项目 Logo 和已有工具图标；新增标准帮助、信息、关闭图标使用 Phosphor 图标库，无品牌图形仿制。
- 文案：使用真实筛选数据；未照搬效果图的数字或公式。费用说明明确估价、账单、价格版本和未匹配价格的区别。

## 交互与验证

- 帮助按钮打开右侧抽屉，关闭按钮和 Escape 可关闭，焦点返回帮助按钮。
- 费用指标信息按钮能打开对应说明并关闭。
- 点击 Pi Agent 来源条可聚焦全部图表，取消聚焦恢复数据。
- 手机 390 × 844 无页面横向溢出；帮助内容和关闭入口完整可见。
- 深色模式正常显示，完成后恢复亮色及原始视口。
- 浏览器未捕获 error/warn。现有 116 项测试及生产构建通过，依赖审计零漏洞。

## Open Questions / 验证边界

- 未进行跨浏览器、真实手机和屏幕阅读器验收。
- 未触发采集或下载导出；这些功能保留原有实现。

## Implementation Checklist

- [x] 说明收纳到帮助抽屉与指标信息入口
- [x] 调整筛选、指标及来源可视化
- [x] 桌面、手机、深色和关键交互验证
- [x] 保留真实数据、额度及现有分析功能

## Follow-up Polish

### 底部明细表格整理（2026-09-14）

- 增加明细标题与行操作提示，统一紫色选中标签、搜索框/清空、导出列表按钮、行间距与分页区。
- 表头固定，排序改成键盘可操作按钮并声明 aria-sort；行支持 Enter/Space 打开明细，返回后保留高亮。
- 浏览器验证搜索空结果/清空、排序、Enter 打开与 Escape 返回、会话下一页 51–100 / 1149（第 2 / 23 页）。390px 下页面无横向溢出，表格内部横滚。
- 构建与 120 项测试通过，控制台无 error/warn；导出数据范围与统计计算未变，未提交推送。

### 热力图统一重绘（2026-09-14）

- 紫色色阶、16px 高圆角网格、可视日期窗口和时段分布统一样式，保留原强度分级算法及逐小时数据。
- 右侧摘要采用轻底色指标和高峰重点卡，星期分布改为七列小柱图；无用量不再标出虚假的峰值日期/时段/星期及周末 100%。
- 悬浮不再缩放格子，支持点按、方向键移动和 Escape 收起；提示框按滚动可见区域定位，横向滚动时清除过期提示。
- 桌面截图与方向键提示通过；390px 右边缘提示框在视口内，页面无横向溢出，网格自身可横滚，摘要位于下方；日期列固定，色阶标签防拆行。
- 构建与 120 项测试通过；本次未新增统计数据，未提交推送。

### 多层同心圆更正（2026-09-14）

- 按用户确认，将单圈扇区改为每来源独立一圈，共用总用量分母；由外到内按用量排序，保留彩色背景轨道和真实占比弧长。
- 圈数增多时自动收窄间距及线宽，中心字号调整避免重叠；整圈轨道支持悬浮与点击，图例保留键盘操作。
- 实际检查 3 来源与全部历史 7 来源半径/线宽，点击聚焦及取消通过；构建与 120 项测试通过。页面留在全部历史便于查看多圈效果。

### 图表统一重绘第一批（2026-09-14）

- 堆叠和分组柱状接入与折线相同的 Bklit 图表容器，新增项目内 SVG 柱形渲染，共用坐标/日期范围/对比与明细；保留旧图异常回退与超过 400 天的兼容路径。
- 来源占比改为项目内 SVG 圆环，使用真实比例，不再强制放大小占比扇区。轻量高亮、中心数值联动、图例列表与键盘聚焦，保留取消聚焦功能；不是直接移植组件库进度环。
- 浏览器验证堆叠和柱状各 90 个柱形（30 天 × 3 来源），坐标为有效 SVG 属性；来源选择和三个来源保留正常，键盘焦点可更新中心信息。
- 深色与 390px 适配已看图，无页面横向溢出，控制台无错误；构建及 120 项测试通过。柱形 tooltip 内容沿用共用实现，未新增真实 hover 的端到端证据。
- 本批不改热力图，不改统计与价格逻辑，无提交推送。

### 收尾检查（2026-09-14）

- 修复导出菜单 Escape / 点击外部关闭；浏览器两条路径均通过。
- 明细打开聚焦关闭按钮，Tab 不离开抽屉，Escape 退出并恢复原入口焦点；浏览器验证打开与 Tab/Escape 路径。
- 修复图例按钮字号继承过大，长模型标题/说明允许换行，避免遮住关闭按钮。
- 无用量筛选实测：模型榜为空、来源与缓存卡明确提示暂无用量，不再暗示 0% 命中率；未写入测试数据。
- 深色桌面与 390px 页面验证无横向溢出，恢复亮色 30 天且无来源筛选；控制台无错误或警告。
- CSV 下载按钮已实际触发，但内置浏览器未报告 download 事件，Downloads 未找到对应文件，不能认定文件下载验收完成。新增 CSV 内容/转义/触发回归测试，和实际落盘验证分开。
- KPI 无 whileHover 位移，但本轮未获得真实连续悬浮帧率证据，仍需用户体感确认；未宣称卡顿彻底解决。
- 正式代码与 examples/ui-showcase 保持目录及依赖隔离；未提交、未推送。

### 筛选详情与导出入口整理（2026-09-14）

- 明细使用当前筛选后的数据，显示日期范围；模型榜跨来源合并时，明细也合并相同模型，表格来源模型行仍保留来源约束。
- 浏览器验证 30 天模型榜与明细均为 20.71 亿、$1,270.11，时间为 2026-08-16 至 2026-09-14。
- 环形图使用未聚焦但已筛选的数据作对照，聚焦后其他来源保留并变淡，图例原生按钮支持键盘且明确标注选中，可在卡片内取消聚焦。
- 趋势标题旁下载按钮移除，原 CSV 导出回调统一从顶部菜单选择“用量明细 / 每日趋势”。菜单展开验证通过，未实际下载文件。
- 手机对比/导出独立操作行；390px 下无页面横向溢出。浏览器无 error/warn，构建与 119 项测试通过。统计及价格计算未改动。

### 模型榜与缓存卡交互细化（2026-09-14）

- 用户澄清反馈针对鼠标悬浮，而非数字切换。已撤销此前淡入替换和仪表时长修改，恢复数字动效；顶部 KPI 移除 whileHover 上移，改为桌面鼠标边框高亮，保留入场动画，避免悬浮边界位移。未采集帧率，不宣称已确认性能根因。

- 模型榜增加排名、位置过渡、NumberFlow 数字、方向提示，原明细入口改为原生按钮，支持 Enter 与可见键盘焦点。
- 缓存卡保留半环仪表，读取/创建分区展示，命中率与预估节省统一数字动效；遵循减少动态效果设置。
- 未变更统计、价格、明细查询范围或来源环形图，未添加数据与新依赖。
- 构建通过，119 项测试通过；浏览器无 error/warn；Enter 打开原模型详情已验证。
- 桌面与 390px 手机宽度实看，两张卡片均无横向溢出；已恢复桌面预览。本轮未另行检查深色模式。

可根据用户实际使用反馈，再决定是否把模型榜与缓存卡改为更宽的两栏布局；本轮不移除现有环比信息。
