services:
  ai-token-dashboard:
    build: .
    ports:
      - "4173:4173"
    environment:
      PORT: "4173"
      INGEST_TOKEN: "${INGEST_TOKEN:?Set INGEST_TOKEN before starting the hub}"
    volumes:
      - ./data:/app/data
